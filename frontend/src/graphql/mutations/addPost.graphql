mutation ($startDate: String!, $endDate: String!, $text: String!, $guests: [String]!){
  addPost(
    startDate: $startDate,
    endDate: $endDate
    text: $text,
    guests: $guests
  ) {
    startDate
    endDate
    text
    guests
    writtenBy {
      name
    }
  }
}